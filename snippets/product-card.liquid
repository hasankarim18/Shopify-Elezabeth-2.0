<div class="product-card">

  <div class="w-72 bg-white shadow-md rounded-xl duration-500 hover:scale-105 hover:shadow-xl">
    <a href="{{ product.url }}">
      <img
        width=""
        height=""
        src="{{ product.featured_image |  image_url }}"
        alt="Product"
        class="h-80 w-72 object-cover rounded-t-xl" />
      <div class="px-4 py-3 w-72">
        <span class="text-gray-400 mr-3 uppercase text-xs">{{ product.vendor }}</span>
        <p class="text-lg font-bold text-black truncate block capitalize">{{ product.title }}</p>
        <div class="flex items-center">
          <p class="text-lg font-semibold text-black cursor-auto my-3">{{ product.price | money }}</p>
          {% if product.compare_at_price_max > product.price %}
            <del>
              <p class="text-sm text-gray-600 cursor-auto ml-2">
                {{ product.compare_at_price_max | money }}
              </p>
            </del>
          {% endif %}

          <div class="ml-2">
            {% render 'icon-add-to-cart'
              , class: "hover:text-green-600" %}
          </div>
        </div>
      </div>
    </a>
  </div>
</div>